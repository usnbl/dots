
# ------------------------------------------------------------------------------
# 1. General
# ------------------------------------------------------------------------------
#Font

fontname "JuliaMono NerdFont:size=11"

# window 
sticky yes                
focus-mode pointer       

# BORDER
borderwidth 3   
color-activeborder "#B1A1E1"    
color-inactiveborder "#D8D1F1" 
color-groupborder "#FF00FF"    
color-urgencyborder "#FF0000"  
color-menubg "#DED3CB"
# GAPS 
gap 5 5 5 5

snapdist 4

moveamount 4  

command kitty "kitty --single-instance"

# ------------------------------------------------------------------------------
# 2. Groups
# ------------------------------------------------------------------------------
autogroup 1 "kitty,sakura,xterm"
autogroup 2 "firefox,chromium,badwolf"
autogroup 3 "Thunar,thunar,nemo,pcmanfm"
autogroup 4 "mpv,celulloid,spotify"

bind-key 4-1            group-only-1
bind-key 4-2            group-only-2
bind-key 4-3            group-only-3
bind-key 4-4            group-only-4
bind-key 4-5            group-only-5
bind-key 4-6            group-only-6
bind-key 4-7            group-only-7
bind-key 4-8            group-only-8
bind-key 4-9            group-only-9
bind-key 4S-1           window-movetogroup-1
bind-key 4S-2           window-movetogroup-2
bind-key 4S-3           window-movetogroup-3
bind-key 4S-4           window-movetogroup-4
bind-key 4S-5           window-movetogroup-5
bind-key 4S-6           window-movetogroup-6
bind-key 4S-7           window-movetogroup-7
bind-key 4S-8           window-movetogroup-8
bind-key 4S-9           window-movetogroup-9
# ------------------------------------------------------------------------------
# 3. KEYMAPS
# ------------------------------------------------------------------------------

bind-key 4-Return "kitty --single-instance"         
bind-key 4-w menu-window       
bind-key 4-d menu-exec        
bind-key 4-s menu-ssh        
# ------------------------------------------------------------------------------
# 4. VIM KEYS
# ------------------------------------------------------------------------------

# --- large movement (Win + hjkl) ---
bind-key 4-h window-move-left-big
bind-key 4-j window-move-down-big
bind-key 4-k window-move-up-big
bind-key 4-l window-move-right-big

# --- short movemente (Win + Ctrl + hjkl) ---
bind-key 4C-h window-move-left
bind-key 4C-j window-move-down
bind-key 4C-k window-move-up
bind-key 4C-l window-move-right

# ------------------------------------------------------------------------------
# 5. Resize
# ------------------------------------------------------------------------------

# --- large resize (Win + Shift + hjkl) ---
bind-key 4S-h window-resize-left-big
bind-key 4S-j window-resize-down-big
bind-key 4S-k window-resize-up-big
bind-key 4S-l window-resize-right-big

# --- short resize (Win + Shift + Ctrl + hjkl) ---
bind-key 4SC-h window-resize-left
bind-key 4SC-j window-resize-down
bind-key 4SC-k window-resize-up
bind-key 4SC-l window-resize-right

# ------------------------------------------------------------------------------
# 6. MODOS DE PANTALLA (FULLSCREEN REAL vs FALSO)
# ------------------------------------------------------------------------------

# FULLSCREEN REAL (Win + f)
bind-key 4-f window-fullscreen

# MAX/ false fullscreen (Win + m)
bind-key 4-m window-maximize

# (Vertical/Horizontal)
bind-key 4S-m window-vmaximize  
bind-key 4C-m window-hmaximize   

# ------------------------------------------------------------------------------
# 7. TILING (yes)
# ------------------------------------------------------------------------------

# Win + t: Tiling Vertical 
bind-key 4-t window-vtile

# Win + Shift + t: Tiling Horizontal 
bind-key 4S-t window-htile

# ------------------------------------------------------------------------------
# 8. Cycle Tab
# ------------------------------------------------------------------------------

bind-key M-Tab window-cycle     # Alt + Tab -> Cycle --->
bind-key 4-Tab window-rcycle    # Win + Tab -> Cycle <----

# ------------------------------------------------------------------------------
# 9. Mouse
# ------------------------------------------------------------------------------

bind-mouse 4-1 window-move
bind-mouse 4-2 window-lower
bind-mouse 4-3 window-resize

# Gestión de Ventanas y Sesión
bind-key 4S-q window-delete  
bind-key 4S-r restart         
bind-key 4S-e quit             

# STICKY (Win + p) 
bind-key 4-p window-stick

# hide /unhide
bind-key 4-period window-hide       #(Win + .)
bind-key 4S-period window-unhide   #(Win + Shift + .)
